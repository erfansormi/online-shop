import { createTheme } from '@mui/material/styles';

export const muiTheme = createTheme({
    palette: {
        primary: {
            main: "#f43f5e",
            dark: "#e11d48",
            light: "#fb7185",
            contrastText: "#fff"
        }
    },
});